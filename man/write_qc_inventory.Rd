% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/qc_dashboard.R
\name{write_qc_inventory}
\alias{write_qc_inventory}
\title{Export a QC inventory as RDS, TSV tables, and a snapshot description}
\usage{
write_qc_inventory(x, output_dir)
}
\arguments{
\item{x}{A \code{bg_qc_inventory} returned by \code{\link[=collect_qc_inventory]{collect_qc_inventory()}}.}

\item{output_dir}{A new or empty output directory. Existing snapshots are
protected from replacement; use a new directory when refreshing a report.}
}
\value{
Invisibly returns the absolute path to the exported directory.
}
\description{
Export a QC inventory as RDS, TSV tables, and a snapshot description
}
\details{
Each data-frame component becomes a TSV with quoted text and \code{NA}
for missing values. \code{inventory.rds} preserves the complete R object;
\code{snapshot.json} records schema, snapshot ID, collection time, and project.
IDs join tables within that snapshot. Reports contain participant IDs and
source paths and should be shared with the same care as their source data.
}
